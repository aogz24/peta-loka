export default function ClusterLocationButton({ onClick }) {
  return (
    <button onClick={onClick} className="cursor-pointer bg-blue-700 text-white rounded-lg shadow-md hover:brightness-90 px-4 py-2">
      Lihat Lokasi
    </button>
  );
}
